import styled from 'styled-components/native';

import { colorSchema } from '~/utils';

export const ChartBar = styled.View`
  width: 25px;
  background-color: ${colorSchema.fontLight};
  border-top-left-radius: 7px;
  border-top-right-radius: 7px;
`;
